\documentclass[tikz, border=10pt]{standalone}
\usepackage{iftex}
\ifluatex
  \usepackage{fontspec}
  \usepackage{unicode-math}
  \setmainfont{Fira Sans}
  \setmonofont{Fira Mono}
  \setmathfont{Fira Math}
\else
  \usepackage{newpxmath}
\fi

\usetikzlibrary{arrows.meta}
\tikzset{every node/.style={font=\Large}}


\begin{document}
\begin{tikzpicture}[scale=1.1, >=Stealth]
  % Colors tuned for dark preview background.
  \colorlet{axiscol}{white}
  \colorlet{gridcol}{white!50!black}

  % Cartesian grid.
  \draw[step=1, very thin, gridcol] (-0.5,-0.5) grid (5.5,5.5);

  % Axes of the frame (O, i, j).
  % \draw[->, thick, axiscol] (-0.3,0) -- (5.6,0) node[below right, text=axiscol] {$x$};
  % \draw[->, thick, axiscol] (0,-0.3) -- (0,4.6) node[above left, text=axiscol] {$y$};

  % Basis vectors.
  \draw[->, very thick, cyan!70!white] (1,0) -- (2,0)
    node[midway, above, text=cyan!70!white] {$\vec{1_x}$};
  \draw[->, very thick, cyan!70!white] (0,2) -- (0,3)
    node[midway, right, text=cyan!70!white] {$\vec{1_y}$};

  % Vector and its endpoint.
  \coordinate (P) at (4,3);
  \coordinate (Q) at (1,1);
  \coordinate (R) at (1,5);
  \coordinate (S) at (5,1);
  % \draw[->, ultra thick, red!85!white] (A) -- (B);
    % node[midway, above left, text=red!85!white] {$\vec{u}$};

  % % Component projections.
  % \draw[dashed, thick, axiscol] (A) -- (4,0);
  % \draw[dashed, thick, axiscol] (A) -- (0,3);

  % Points and labels.
  \fill[axiscol] (0,0) circle (1.8pt);
  \fill[red!85!white] (P) circle (2pt);
  \fill[red!85!white] (Q) circle (2pt);
  \fill[red!85!white] (R) circle (2pt);
  \fill[red!85!white] (S) circle (2pt);
  \node[below left, text=axiscol] at (0,0) {$O$};
  \node[above right, text=red!85!white] at (P) {$P$};
  \node[below left, text=red!85!white] at (Q) {$Q$};
  \node[above left, text=red!85!white] at (R) {$R$};
  \node[below right, text=red!85!white] at (S) {$S$};
  % \node[below, text=axiscol] at (2,0) {$4$};
  % \node[left, text=axiscol] at (0,1.5) {$3$};

  % Decomposition in the basis.
  % \node[text=axiscol] at (3.8,4.1) {$\vec{u}=4\vec{i}+3\vec{j}$};
\end{tikzpicture}
\end{document}
